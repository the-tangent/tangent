<div class="page-header">
  <div class="article-controls pull-right">
    <%= erb :article_controls, :locals => { :id => article.id, :published => article.published? }%>
  </div>

  <h3><%= article.title %> <small><%= article.author %></small></h3>
</div>

<img src="<%= article.image_url %>">

<div>
  <%= article.content %>
</div>
