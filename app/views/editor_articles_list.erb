<a href="<%= path %>?published=false">Unpublished</a>
<a href="<%= path %>?published=true">Published</a>

<% articles.each do |article| %>
    <h4>
      <a href="/editor/articles/<%= article.id %>">
        <%= article.title %>
      </a> <small><%= article.author %></small></h4>
<% end %>
